NODE_ENV=development
PORT=4000

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=

# Comma-separated origins for CORS (use * only in local dev)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Temporary MVP auth: seeded admin UUID (or leave empty and use Bearer header)
# Replace with session + email 2FA when integrating existing auth
DEV_AUTH_USER_ID=11111111-1111-1111-1111-111111111111
